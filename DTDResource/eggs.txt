Easter eggs

CombatFrame
+            if i == "Kyle The Destroyer":
                self.currentFloor.monsters[count].monsteratk = self.master.destroyerAtk

Zacks menu

+self.builder.get_object('titleCanvas').bind("<Button-2>", self.destroyerAtkUp)


+	def destroyerAtkUp(self, event):
		self.master.destroyerAtk += 20

NewGame

+            if self.playerName.lower() == "i am a box" and not self.boxEggFound:
                self.master.player = PlayerBuilder().build("Box", self.playerName)
                messagebox.showinfo("Easter Egg!", "YOU ARE NOW A BOX!")
                self.classList.insert(0, "Box")
                self.boxEggFound = True

            else:
                